# Frontend Environment Variables
EXPO_PUBLIC_API_URL=http://localhost:3001/api
EXPO_PUBLIC_APP_ENV=development

# Backend Environment Variables
PORT=3001
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/challengeu
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Frontend Notes:
# Variables prefixed with `EXPO_PUBLIC_` are accessible in the app.
# Other variables are only accessible during build time.

# Backend Notes:
# Create a .env file in the root directory with the backend variables above.
